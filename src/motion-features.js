/* Fonctionnalités framer-motion (domMax : drag + layout), chargées en
   asynchrone APRÈS le premier rendu — sorties du bundle initial. */
import { domMax } from 'framer-motion';

export default domMax;
